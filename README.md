# ðŸŒ¿ Loka

AI router lokal, zero-dependency. Satu endpoint OpenAI-compatible untuk semua provider.

## Jalankan
node index.js

Dashboard: http://localhost:8787

## Config
Edit loka.json â€” tambah provider, combo, client key.

## Endpoint
- POST /v1/chat/completions
- POST /v1/messages
- GET /v1/models
- GET /health
- GET /dashboard
